rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // --- MODO DE DESENVOLVIMENTO (TEST MODE) ---
    // Permite upload e download de arquivos para qualquer usuário.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}